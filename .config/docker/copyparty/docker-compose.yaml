services:
  copyparty:
    image: copyparty/ac:latest
    container_name: copyparty
    ports:
      - 3923:3923
    volumes:
      - /home/ok/docker/copyparty/conf:/cfg/config.conf:ro
      - /home/ok/docker/copyparty/db:/cfg/hists #database files
      - /home/ok/media:/media #files being exposed to copyparty
    restart: unless-stopped
